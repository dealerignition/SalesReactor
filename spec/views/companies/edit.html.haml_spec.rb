require 'spec_helper'

describe "companies/edit.html.haml" do
  before do
    assign(:company, stub_model(Company))
    render
  end

  pending

  it "should display the company edit form" do
  end
end
